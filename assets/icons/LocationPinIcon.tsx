// The location pin glyph — currently identical to LogoIcon but kept as a
// separate export so that design can diverge later (e.g. a filled brand pin
// for the logo vs. an outlined map marker) without touching every callsite.
export { default } from "@/assets/icons/LogoIcon";
